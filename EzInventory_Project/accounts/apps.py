from django.apps import AppConfig


class AccountsConfig(AppConfig):
    """Represent accounts application.

    Keyword arguments:
    AppConfig -- class representing a Django application and its configuration
    """

    name = 'accounts'
